using UnityEngine;

public class ejercicio2 : MonoBehaviour
{
    int numero = 20;
    void Start()
    {
        while (numero >= 0) ;
        Debug.Log(numero);
        numero--;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
