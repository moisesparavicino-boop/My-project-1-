using UnityEngine;

public class ejercicio1 : MonoBehaviour
{
    int numeros = 1;
    void Start()
    {
        while (numeros <= 10) ;
        Debug.Log(numeros);
        numeros++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
