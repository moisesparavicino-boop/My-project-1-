using UnityEngine;

public class ejercicio3 : MonoBehaviour
{
    int contador = 1;
    void Start()
    {
        while (contador <= 5) ;
        Debug.Log("Jugador conectado");
        contador++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
